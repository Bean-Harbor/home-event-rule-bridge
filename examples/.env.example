# Telegram bot token from BotFather.
TELEGRAM_BOT_TOKEN=

# Optional comma-separated chat ids. Leave empty while testing locally.
TELEGRAM_ALLOWED_CHAT_IDS=

# Home Assistant URL and long-lived access token.
HA_URL=http://homeassistant.local:8123
HA_TOKEN=

# Default parser is local rule-based. For a local LLM endpoint, use openai-compatible.
NSP_PROVIDER=rules
OPENAI_COMPAT_BASE_URL=http://localhost:11434/v1
OPENAI_COMPAT_API_KEY=local
OPENAI_COMPAT_MODEL=qwen3:1.7b

# Safe default: dry-run only. Set true only when you mounted HA_CONFIG_DIR.
ALLOW_WRITE_AUTOMATIONS=false
HA_CONFIG_DIR=

